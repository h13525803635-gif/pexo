# Pexo 项目 93782494601 — 成片问题根因分析

## 问题描述

项目 93782494601 的成片过程中出现了多个严重问题，导致用户体验极差：
1. **渲染队列严重积压** — 从提交渲染到最终交付耗时超过 2 小时
2. **LLM 无限轮询无退出机制** — Agent 陷入 sleep/query_render 死循环
3. **渲染任务过期** — 渲染 job 在等待过程中过期，需要多次重新提交
4. **无音频轨道** — 最终成片无 BGM
5. **第一版 warning 未修复** — root_composition_missing_data_start

## 项目基本信息

| 字段 | 值 |
|------|------|
| 项目 ID | 93782494601 |
| Session ID | thread_75127832422_93782494601_01KTT877KNMS3XQ6WT553XV5QR |
| 环境 | test |
| Trace 数量 | 3 |
| 总执行时间 | 02:29:45 ~ 05:04:28 UTC（约 2.5 小时） |
| 使用模型 | ChatBedrock (Claude Sonnet) |
| 最终输出 | beauty_lab_celebration_v2.mp4 (asset_id: a_c8udape) |

## 用户请求

**初始请求**：

> "Create a celebration video for a livestream host using 01_ecommerce/07_livestream_host.png. Add a 'BEAUTY LAB' logo watermark in the top right corner at 80% opacity. At 00:02, fade in a dynamic growing bar chart in the center showing sales data: 'June 1: $500k / June 2: $1.2M / June 3: $3M'. After the chart animation finishes, freeze it on the last frame until the end of the video."

**追加请求**（Trace 3）：

> "The character in the background image also needs regular live-stream sales footage."

## 三次执行 Trace 概览

| Trace | 时间范围 | Observations | 核心操作 | 结果 |
|-------|---------|-------------|---------|------|
| 1 (b6a0e474) | 02:29 ~ 02:32 | 163 | HyperFrames 合成静态背景版本 | 成功交付 v1 |
| 2 (a8c5a0e7) | 02:46 ~ 03:56 | 602 | video_generate + 重建合成 + 渲染等待 | 渲染超时未交付 |
| 3 (03ef4565) | 03:56 ~ 05:04 | 512 | 继续等待 + 重提交 + 最终交付 | 成功交付 v2 |

## 问题一：渲染队列严重积压（核心问题）

### 现象

| 时间点 | 事件 | 等待时间 |
|--------|------|---------|
| 02:54:56 | 第一次成功 submit_render (job: 8zfbssjn) | estimated_wait: 132s |
| 03:13:56 | 判断 stalled，重新提交 (job: 1p3no4t9) | estimated_wait: 252s |
| 04:01:11 | 判断 stalled，再次重新提交 (job: 6vgcnmwi) | estimated_wait: 384s |
| 04:47:30 | 渲染任务过期(expired)，第三次重新提交 (job: 0xfbivmc) | estimated_wait: 42s |
| 04:59:58 | 渲染开始运行 | - |
| 05:04:24 | 渲染完成 | - |

**从首次提交到最终完成：2 小时 10 分钟。**

### 根因

渲染管线（HyperFrames render pipeline）在该时段出现严重的队列积压。从 `estimated_wait_seconds` 的变化可以看出：
- 第一次提交时预估 132s，实际等了 19 分钟仍未开始
- 第二次提交预估 252s（4.2 分钟），实际等了 47 分钟仍未开始
- 第三次提交预估 384s（6.4 分钟），实际等了 46 分钟后过期
- 第四次提交预估 42s，实际约 17 分钟后开始运行

**根本原因是渲染服务的容量不足以应对当时的负载。**

### 影响

- 用户等待超过 2 小时才收到最终视频
- LLM 消耗了大量 token 在无意义的 sleep/query_render 循环中
- Trace 2 因为等待时间过长被中断，需要 Trace 3 继续

## 问题二：LLM 无限轮询无退出机制

### 现象

在 Trace 2 和 Trace 3 中，LLM 的行为模式完全相同：
```
sleep(120) → query_render → status: pending → sleep(120) → query_render → ...
```

Trace 2 中这个循环持续了 **60 分钟**（02:54 ~ 03:56），Trace 3 中持续了 **67 分钟**（03:56 ~ 05:04）。

### 根因

**系统提示词和 hyperframes-skill 中没有定义渲染超时退出策略。** LLM 没有以下关键规则：
- 最大等待时间限制（如 10 分钟）
- 超时后的降级策略（如通知用户、返回中间产物）
- 渲染失败/过期后的最大重试次数

### 影响

- 大量无效 token 消耗（Trace 2: 101 个 generations，Trace 3: 86 个 generations）
- 用户体验极差（2+ 小时等待）
- Agent 资源被长时间占用

## 问题三：渲染任务过期

### 现象

Job `6vgcnmwi` 在 04:01 提交，04:47 被系统判定为过期（expired），等待了 46 分钟。

### 根因

渲染管线对排队任务有最大等待时间限制。当队列积压严重时，长时间未被调度的任务会被自动过期清理。

### 影响

LLM 需要重新提交渲染，进一步延长了总等待时间。

## 问题四：无音频轨道

### 现象

最终交付的 `beauty_lab_celebration_v2.mp4` 无 BGM。

### 根因分析

这个案例比较特殊：
1. LLM 使用 `video_generate` (seedance reference2video) 生成了带音频的直播主播视频（probe_media 确认有音频流）
2. 但 HyperFrames 渲染管线在合成最终视频时，**不会保留嵌入 `<video>` 元素的音频轨道**
3. LLM 在交付前没有对最终视频进行 `probe_media` 验证

**根因**：HyperFrames 渲染管线默认只输出视频流，不处理嵌入媒体的音频。LLM 也没有在交付前验证最终产物是否包含音频。

### 影响

用户得到的视频完全静音，尽管 AI 生成的直播画面本身是有声音的。

## 问题五：第一版 Warning 未修复

### 现象

Trace 1 中 submit_render 返回 warning：
> `[root_composition_missing_data_start] Root composition "beauty-lab-celebration" is missing data-start.`

LLM 忽略了这个 warning，直接等待渲染完成并交付。

### 根因

同项目 68295072259 的问题：LLM 将 `success: true` 视为"可继续"，忽略 warnings。

**值得注意的是**：在 Trace 2 中，LLM 在修改 HTML 时主动修复了这个问题（添加了 `data-start="0"`），说明 LLM 有能力修复但在第一次没有这么做。

## 解决方案

### 问题一（渲染队列积压）解决方案

#### 短期方案：渲染超时退出策略

在 hyperframes-skill 中增加超时规则：

```
## Render Timeout Policy

1. Maximum wait time: 10 minutes from submit_render
2. If render is still "pending" after 10 minutes:
   - Notify user: "Render queue is busy, estimated wait is X minutes"
   - Ask user if they want to continue waiting or receive intermediate assets
3. Maximum total retries: 2 (after initial submit)
4. If all retries fail or expire:
   - Deliver intermediate assets (preview frames + source HTML)
   - Inform user that rendering will be completed asynchronously
```

#### 中期方案：异步渲染通知

实现渲染完成后的异步通知机制：
- 提交渲染后立即告知用户预计等待时间
- 渲染完成后通过消息推送通知用户
- 避免 Agent 长时间占用资源轮询

#### 长期方案：渲染服务扩容

- 增加渲染节点的自动伸缩能力
- 实现优先级队列（付费用户优先）
- 添加渲染任务的实时状态面板

### 问题二（无限轮询）解决方案

在系统提示词中增加轮询限制规则：

```
## Polling Limits

When waiting for render completion:
- Use exponential backoff: 15s → 30s → 60s → 120s (max)
- Maximum polling iterations: 10
- Maximum total wait time: 10 minutes
- After hitting any limit, MUST stop polling and:
  1. Inform user of the delay
  2. Provide available intermediate results
  3. Do NOT start another trace just to continue polling
```

### 问题三（渲染过期）解决方案

- 在 `query_render` 返回 expired 状态时，LLM 应该检查当前队列状态再决定是否重试
- 如果 `estimated_wait_seconds` 仍然很高（>300s），应该通知用户而非盲目重试

### 问题四（无音频）解决方案

#### 方案 A：HyperFrames 渲染管线支持音频

在渲染管线中增加音频混合能力：
- 自动提取嵌入 `<video>` 元素的音频轨道
- 在最终输出中混合所有音频源

#### 方案 B：后处理添加音频

在 show_final_video 之前增加必要步骤：
```
## Pre-delivery Checklist

Before calling show_final_video:
1. probe_media the rendered video
2. If no audio stream detected:
   a. If composition contains <video> with audio → extract and mix using assembly-skill
   b. If no embedded audio source → generate BGM using music_generate
3. Verify final video has both video and audio streams
```

### 问题五（Warning 未修复）解决方案

同项目 68295072259 的建议：在 hyperframes-skill 中增加 warning 分级处理规则。

## 执行时间线总览

| 时间 (UTC) | Trace | 事件 |
|-----------|-------|------|
| 02:29:45 | 1 | 开始执行初始请求 |
| 02:31:19 | 1 | submit_render (warning: missing data-start) |
| 02:32:49 | 1 | 交付 v1 (静态背景版本) |
| 02:46:04 | 2 | 用户追加请求"需要直播画面" |
| 02:47:03 | 2 | video_generate (seedance reference2video) |
| 02:53:42 | 2 | probe_media 确认生成视频有音频 |
| 02:54:37 | 2 | submit_render 失败 (lint: media_missing_data_start) |
| 02:54:43 | 2 | 修复后再次 submit 失败 (lint: media_missing_id) |
| 02:54:56 | 2 | 第三次 submit 成功 (job: 8zfbssjn) |
| 03:13:56 | 2 | 等待 19 分钟后重新提交 (job: 1p3no4t9) |
| 03:56:10 | 2 | 等待 42 分钟仍未完成，发送中间产物 |
| 03:56:57 | 3 | 继续轮询 job 1p3no4t9 |
| 04:01:11 | 3 | 判断 stalled，重新提交 (job: 6vgcnmwi) |
| 04:47:30 | 3 | 渲染过期，再次重新提交 (job: 0xfbivmc) |
| 04:59:58 | 3 | 渲染开始运行 |
| 05:04:24 | 3 | 渲染完成，交付 v2 |

## 总结

| 维度 | 问题 | 根因 | 影响 | 优先级 |
|------|------|------|------|--------|
| 渲染积压 | 等待 2+ 小时 | 渲染服务容量不足 | 用户体验极差 | **P0** |
| 无限轮询 | Agent 死循环 | 无超时退出策略 | token 浪费、资源占用 | **P0** |
| 任务过期 | 渲染 job expired | 队列积压导致超时 | 需要重新提交 | P1 |
| 无音频 | 最终视频静音 | HyperFrames 不保留嵌入音频 | 成片不完整 | P1 |
| Warning 未修复 | data-start 缺失 | LLM 忽略 warning | 播放行为可能异常 | P2 |

**最关键的改进点**：为 Agent 添加渲染超时退出机制和异步通知能力，避免在渲染服务不可用时陷入无意义的长时间等待。
