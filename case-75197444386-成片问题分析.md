# Pexo 项目 75197444386 — 成片问题根因分析

## 问题描述

项目 75197444386 在成片过程中出现了多个问题：
1. **多次 Lint 失败** — 提交渲染前经历了 4 次 lint 错误才通过
2. **渲染队列严重积压** — 两个渲染任务均长时间卡在 pending 状态
3. **渲染任务过期** — job 从 Trace 1 跨越到 Trace 2 才最终完成（间隔 3+ 小时）
4. **Warning 未修复** — `timed_element_missing_clip_class` 在所有 3 次成功提交中均存在
5. **无音频轨道** — 最终成片无 BGM
6. **asset 引用路径混乱** — LLM 在重新提交时使用了错误的路径格式

## 项目基本信息

| 字段 | 值 |
|------|------|
| 项目 ID | 75197444386 |
| Session ID | thread_75127832422_75197444386_01KTT8H16PWR5EJ9QXVNNAE9DZ |
| 环境 | test |
| Trace 数量 | 2 |
| 总执行时间 | 02:35:07 ~ 06:40:25 UTC（约 4 小时） |
| 使用模型 | ChatBedrock (Claude Sonnet) |
| 最终输出 | factory_vintage_final.mp4 (asset_id: a_TJ18GLf) |

## 用户请求

> "Generate a video of a modern factory in operation using 03_brand/02_factory_line.png. Overlay a vintage film grain and noise effect across the entire video. From 00:05 to 00:10, slowly fade in and out a black-and-white historical photo of an old factory in the center of the screen. Keep a permanent watermark text 'Est. 1998' in the bottom right corner."

## 两次执行 Trace 概览

| Trace | 时间范围 | Observations | 核心操作 | 结果 |
|-------|---------|-------------|---------|------|
| 1 (6130a2e5) | 02:35 ~ 03:27 | 430 | video_generate + image_generate + HyperFrames 合成 + 渲染等待 | 渲染超时未交付 |
| 2 (8d0523663) | 06:38 ~ 06:40 | 74 | 发现 job 过期 → 重新提交 → 2 分钟完成 → 交付 | 成功交付 |

## 问题一：多次 Lint 失败（4 次失败才通过）

### 现象

| 次数 | 时间 | Lint 错误 | LLM 修复动作 |
|------|------|----------|-------------|
| 1 | 02:45:17 | 3 个错误：`media_missing_data_start` + `gsap_from_opacity_noop` + 第三个错误 | 修复 watermark opacity、重组 HTML 结构 |
| 2 | 02:46:45 | 2 个错误：`video_nested_in_timed_element` + `media_missing_id` | 移除嵌套的 data-start、添加 video id |
| 3 | 02:47:07 | 1 个错误：`media_missing_data_start` | 重构 HTML 将 video 移为 root 直接子元素 |
| 4 | 02:47:36 | **通过**（1 个 warning：`timed_element_missing_clip_class`） | - |

### 根因

**LLM 对 HyperFrames 的 HTML 结构规范理解不足。** 核心矛盾在于：
- `<video>` 元素需要 `data-start` 属性（否则 HyperFrames 无法管理播放）
- 但 `<video>` 不能嵌套在已有 `data-start` 的父元素中（否则会被冻结）
- `<video>` 还必须有 `id` 属性（渲染器通过 id 发现媒体元素）

LLM 逐步修复每个错误，但每次修复又引入新错误，形成"打地鼠"模式。最终在第 4 次提交时通过重构 HTML 结构才解决。

### 影响

- 4 次 submit_render 调用（前 3 次失败）
- 约 2.5 分钟浪费在反复修复 lint 错误上
- 增加了 token 消耗

## 问题二：渲染队列严重积压

### 现象

| 时间点 | 事件 | 等待时间 |
|--------|------|---------|
| 02:47:36 | 第一次成功提交 (job: cjjbpfh1, estimated_wait: 70s) | - |
| 03:09:27 | 仍在 pending（等待 22 分钟） | 22 min |
| 03:10:09 | 判断 stuck，决定重新提交 | - |
| 03:11:31 | 第二次成功提交 (job: 7pkvm5qm, estimated_wait: 228s) | - |
| 03:27:48 | 两个 job 均 stuck in pending | 16 min |
| 03:27:48 | Trace 1 结束（未交付） | - |
| 06:38:26 | Trace 2 开始，查询 job 7pkvm5qm → expired | 3+ hours |
| 06:38:38 | 第三次提交 (job: 4hnr4xot, estimated_wait: 6s) | - |
| 06:40:11 | 渲染完成 | ~2 min |

**从首次提交到最终完成：约 3 小时 53 分钟。**

### 根因

与项目 93782494601 相同：渲染管线在该时段（02:47 ~ 03:27 UTC）出现严重的队列积压。Trace 2 在 06:38 时队列已恢复正常（estimated_wait 仅 6s），2 分钟即完成渲染。

## 问题三：Asset 引用路径混乱

### 现象

Trace 1 在重新提交渲染时，LLM 尝试了 3 种错误的路径格式：

| 尝试 | 路径格式 | 结果 |
|------|---------|------|
| 1 | `file:///projects/75197444386/workspace/factory_vintage.html` | ENOENT 错误 |
| 2 | `asset://factory_vintage.html` | Invalid asset_id 错误 |
| 3 | `asset://a_k8ivr4N`（正确） | 成功 |

### 根因

**LLM 对 submit_render 的 `html_file` 参数格式理解不一致。** 正确格式应该是 `asset://{asset_id}`，但 LLM 先后尝试了 `file://` 路径和文件名作为 asset_id。

此外，LLM 发现 HTML 文件不在 workspace/assets 目录中（因为之前用 `write_file` 写到了 workspace 根目录），需要重新写入 assets 目录才能获得 asset_id。

### 影响

- 2 次额外的 submit_render 失败
- 需要重新写入 HTML 文件
- 增加了整体执行时间

## 问题四：Warning 未修复

### 现象

所有 3 次成功的 submit_render 都返回了相同的 warning：

> `[timed_element_missing_clip_class] <div id="grain-layer"> has timing attributes but no class="clip". The element will be visible for the entire composition instead of only during its scheduled time range.`

LLM 在第一次提交时明确表示："The warnings are non-blocking (elements spanning full 15s anyway so no visibility issue)"，认为 grain-layer 本身就是全时长显示的，所以 missing clip class 不影响效果。

### 根因

**LLM 的判断在此案例中是合理的** — grain-layer 的 `data-duration="15"` 等于视频总时长，所以有无 `class="clip"` 在视觉效果上确实没有区别。但从代码规范角度，仍应添加 `class="clip"` 以保持一致性。

### 影响

- 此案例中无实际视觉影响
- 但如果后续修改视频时长，可能导致 grain-layer 行为异常

## 问题五：无音频轨道

### 现象

最终交付的 `factory_vintage_final.mp4` 无 BGM。Trace 2 在交付前没有调用 `probe_media` 验证最终视频。

### 根因

与之前项目相同的问题：
1. 用户请求未提及音频
2. HyperFrames 渲染不保留嵌入视频的音频
3. LLM 不主动添加 BGM
4. 交付前无 probe_media 验证

### 影响

用户得到的视频完全静音。

## 解决方案

### 问题一（多次 Lint 失败）解决方案

#### 方案 A：增强 HyperFrames HTML 模板

在 hyperframes-skill 中提供标准的 HTML 模板，特别是视频元素的正确结构：

```html
<!-- 正确的视频元素结构 -->
<div id="root" data-composition-id="..." data-width="..." data-height="...">
  <!-- 视频必须是 root 的直接子元素，不能嵌套在其他 timed element 中 -->
  <video id="bg-video" 
         src="asset://..." 
         data-start="0" 
         data-duration="15" 
         data-track-index="0"
         muted playsinline>
  </video>
  <!-- 其他叠加层 -->
  <div id="overlay" class="clip" data-start="0" data-duration="15" data-track-index="1">
    ...
  </div>
</div>
```

#### 方案 B：在 skill 中明确列出 video 元素的 3 个必要条件

```
## Video Element Rules (MUST follow all 3):
1. MUST have `id` attribute (renderer discovers media by id)
2. MUST have `data-start` attribute (HyperFrames needs it for playback control)
3. MUST NOT be nested inside any element that also has `data-start` (causes FROZEN video)
→ Solution: Always place <video> as direct child of root composition div
```

### 问题二（渲染队列积压）解决方案

同项目 93782494601 的建议：
- 增加渲染超时退出策略（最大等待 10 分钟）
- 实现异步渲染通知机制
- 渲染服务扩容

### 问题三（Asset 路径混乱）解决方案

在 hyperframes-skill 中明确 submit_render 的参数规范：

```
## submit_render Parameter Rules:
- `html_file` MUST use format: `asset://{asset_id}` (e.g., asset://a_k8ivr4N)
- To get asset_id: use write_file to save HTML to workspace/assets/ directory, 
  then use get_file_info to retrieve the asset_id
- NEVER use `file://` paths or bare filenames
- NEVER use file names as asset_id (asset_id format: a_{alphanumeric})
```

### 问题四（Warning 未修复）解决方案

虽然此案例中 LLM 的判断是合理的（grain-layer 全时长显示），但建议：
- 始终添加 `class="clip"` 到有 timing 属性的元素，保持代码一致性
- 在 skill 中说明：即使元素时长等于总时长，也应添加 clip class

### 问题五（无音频）解决方案

同之前项目的建议：
- 系统提示词增加默认音频规则
- 交付前必须 probe_media 验证

## 执行时间线总览

| 时间 (UTC) | Trace | 事件 |
|-----------|-------|------|
| 02:35:07 | 1 | 开始执行 |
| 02:35:22 | 1 | analyze_file_content 分析参考图 |
| 02:36:28 | 1 | 并行调用 video_generate (seedance) + image_generate (seedream) |
| 02:43:42 | 1 | probe_media 确认生成视频 (1280x720, h264) |
| 02:44:56 | 1 | write_file 创建 HTML 合成文件 |
| 02:45:17 | 1 | submit_render #1 失败 (3 lint errors) |
| 02:46:45 | 1 | submit_render #2 失败 (2 lint errors) |
| 02:47:07 | 1 | submit_render #3 失败 (1 lint error) |
| 02:47:36 | 1 | submit_render #4 成功 (job: cjjbpfh1, 1 warning) |
| 03:09:27 | 1 | 等待 22 分钟仍 pending |
| 03:10:09 | 1 | 判断 stuck，决定重新提交 |
| 03:10:37 | 1 | submit_render #5 失败 (file:// 路径错误) |
| 03:11:20 | 1 | submit_render #6 失败 (invalid asset_id) |
| 03:11:31 | 1 | submit_render #7 成功 (job: 7pkvm5qm) |
| 03:27:48 | 1 | 两个 job 均 stuck，Trace 1 结束 |
| 06:38:26 | 2 | Trace 2 开始，查询 job → expired |
| 06:38:38 | 2 | submit_render #8 成功 (job: 4hnr4xot, estimated: 6s) |
| 06:40:11 | 2 | 渲染完成 |
| 06:40:25 | 2 | 交付 factory_vintage_final.mp4 |

## 总结

| 维度 | 问题 | 根因 | 影响 | 优先级 |
|------|------|------|------|--------|
| Lint 失败 | 4 次失败才通过 | LLM 不熟悉 video 元素的 3 个必要条件 | 时间浪费、token 消耗 | **P0** |
| 渲染积压 | 等待 3+ 小时 | 渲染服务容量不足 | 用户体验极差 | **P0** |
| 路径混乱 | 3 种错误路径格式 | LLM 不清楚 submit_render 参数规范 | 额外失败和重试 | **P1** |
| Warning 未修复 | grain-layer 缺少 clip class | LLM 判断无影响（此案例合理） | 代码规范问题 | P2 |
| 无音频 | 最终视频静音 | 系统无默认音频规则 | 成片不完整 | P1 |

**最关键的改进点**：
1. 在 hyperframes-skill 中提供 video 元素的标准模板和 3 条必要规则，避免反复 lint 失败
2. 明确 submit_render 的 `html_file` 参数必须使用 `asset://{asset_id}` 格式
