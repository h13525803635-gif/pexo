# Pexo 项目 78043099247 — 46 秒处人声音频重叠问题根因分析

## 问题描述

项目 78043099247 在成片约 **46 秒处开始出现人声音频重叠**：独立的 voiceover 旁白与视频素材内嵌的人声/环境音同时播放，导致音频混乱。

## 项目基本信息

| 字段 | 值 |
|------|------|
| 项目 ID | 78043099247 |
| Session ID | thread_75127832422_78043099247_01KVA4JYMTR6G5BBAXJQ0SRM6P |
| 环境 | test |
| Trace 数量 | 2 |
| 创建时间 | 2026-06-17T06:34:05 UTC |
| 视频类型 | 供应链主题视频（Supply_Chain_Disruptions_60s，约 60 秒） |
| 最终输出 | Supply_Chain_Disruptions_60s.mp4 |

## 视频结构

该项目包含 5 个视频段和 4 个独立 voiceover 旁白音频：

| 视频段 | 时间范围 | 对应 Voiceover |
|--------|---------|---------------|
| v1 | 0s ~ 约 9s | vo_seg1 |
| v2 | 约 9s ~ 约 18s | vo_seg2 |
| v3 | 约 18s ~ 约 27s | vo_seg3_impact |
| v4 | 约 27s ~ 约 34s | — |
| **v5 (seg4_recovery)** | **34.34s ~ 59.4s** | **vo_seg4_recovery** |

**46 秒正好落在 v5 的时间范围内（34.34s ~ 59.4s）。**

## 根因分析

### 直接原因

视频段 v5（seg4_recovery）的 `muted` 属性被移除，导致其内嵌人声/环境音与独立 voiceover 音轨 `vo_seg4_recovery` 在 46 秒处同时播放。

### 详细过程

**第一步：LLM 生成 voiceover 和视频素材**

LLM 在 Trace 1 中通过 TTS 生成了 4 段 voiceover 旁白：
- `vo_seg1` — 第 1 段旁白
- `vo_seg2` — 第 2 段旁白
- `vo_seg3_impact` — 第 3 段旁白（影响篇）
- `vo_seg4_recovery` — 第 4 段旁白（恢复篇）

同时通过 `video_generate` 生成了 5 段视频素材，这些视频**自带音频轨道**。

**第二步：HTML 合成**

LLM 在 Trace 2 中创建 HTML 合成文件 `supply_chain_video.html`，将 voiceover 作为独立 `<audio>` 元素嵌入，视频作为 `<video>` 元素嵌入，初始状态为 `muted playsinline`。

**第三步：Lint 错误触发错误修复**

第一次 `submit_render`（06:52:15）返回 lint 错误：

> `[video_muted_with_declared_audio]` — `<video>` declares `data-has-audio="true"` but also has `muted`. Studio preview will silence the video audio.

**第四步：LLM 选择了错误的修复方向**

LLM 在 06:59:32 的 `edit_file` 中修改了 v5：

```diff
- <video id="v5" class="bg-vid clip" src="..." 
-   data-start="34.34" data-duration="25.06" data-track-index="0" muted playsinline>
+ <video id="v5" class="bg-vid clip" src="..." 
+   data-start="34.34" data-duration="25.06" data-track-index="0" playsinline>
```

**移除了 `muted`**，使视频内嵌音频被启用。但此时 HTML 中已有 `vo_seg4_recovery` 作为独立 `<audio>` 元素在同一时间段播放。

**第五步：渲染通过并交付**

最终 `submit_render`（06:59:37）lint 通过，渲染成功。但成片中 34.34s~59.4s 区间内两路人声同时播放。

## 与项目 49169912420/74524136316 的区别

| 维度 | 49169912420 / 74524136316 | **78043099247** |
|------|--------------------------|-----------------|
| 重叠类型 | BGM + 视频原声 | **人声 voiceover + 视频内嵌人声** |
| 重叠范围 | 全时长 | **仅 46s 开始（v5 区间）** |
| 触发原因 | 所有视频统一移除 muted | **仅 v5 被移除 muted**（可能前几段视频无明显音频） |
| 感知严重度 | 背景音乐混杂 | **两路人声同时说话，极其刺耳** |
| 根本问题 | 系统缺乏音频互斥规则 | **LLM 在 lint 修复的二选一中选错方向** |

## 解决方案

### 方案一（推荐）：明确 Lint 修复策略

在 hyperframes-skill 中增加明确的修复指导：

```markdown
## 修复 [video_muted_with_declared_audio] 的决策树

当遇到此 lint 错误时，检查 HTML 中是否存在独立音频轨道：

IF 同一时间段内存在 <audio> 元素（BGM 或 voiceover）:
  → 移除 data-has-audio 属性（保留 muted）
  → 理由：独立音频轨道优先，视频内嵌音频应被静音

ELSE IF 不存在独立音频轨道:
  → 移除 muted 属性（保留 data-has-audio）
  → 理由：视频内嵌音频是唯一音源，应被启用
```

### 方案二：增加渲染前音频冲突检测

在 `submit_render` 的 lint 阶段增加新规则：

```
[audio_voice_overlap] — ERROR
Unmuted <video id="v5"> (34.34s-59.4s) overlaps with <audio id="vo_seg4"> 
(34.34s-59.4s). Two voice sources will play simultaneously.
Action: Add `muted` to the <video> element or remove the overlapping <audio>.
```

### 方案三：Voiceover 生成后自动标记视频静音

当 LLM 通过 TTS 生成了 voiceover 并计划嵌入 HTML 时，自动将对应时间段的 `<video>` 元素标记为 `muted`，不依赖后续 lint 修复。

## 总结

| 维度 | 说明 |
|------|------|
| 问题类型 | 人声 voiceover 与视频内嵌人声在 46s 处重叠 |
| 根因 | LLM 修复 lint `[video_muted_with_declared_audio]` 时，错误地移除了 v5 的 `muted` 而非移除 `data-has-audio` |
| 为什么选错 | Lint 错误描述只说"muted 和 data-has-audio 冲突"，未指导在有独立音频时应保留 muted |
| 影响范围 | 所有使用 voiceover + 带音频视频素材的 HyperFrames 合成项目 |
| 优先级 | **P0** — 人声重叠比 BGM 重叠更刺耳，用户感知极差 |
| 推荐方案 | 在 hyperframes-skill 中明确 lint 修复决策树 + 增加音频冲突检测 lint 规则 |
