# 项目 42917497221 — 版权问题导致同一视频中前后角色不一致

## 问题描述

项目 42917497221（"Curiosity: Your Superpower to Explore and Learn"）是一个约 2.5 分钟的儿童教育视频，包含 7 个场景。用户提供了主角 Alex 的角色参考图（Pixar 风格 3D CGI 男孩，蓝色衬衫、棕色头发、棕色眼睛）。成片中 Alex 在不同场景间外观明显不一致——部分场景与参考图匹配，部分场景角色外观完全不同。

## Langfuse 追踪时间线

| 时间 (UTC) | 操作 | 结果 |
|------------|------|------|
| 23:24:18 | write_todos | 创建任务计划 |
| 23:26:10 | image_generate (end_card) | 成功 |
| 23:26:58 | video_generate × 7（并行，使用参考图） | scene4 成功；scene1/2/3/5/6 失败 |
| 23:32:58 | video_generate × 5（第一次重试，移除参考图） | scene2/3 成功；scene1/5/6 仍失败 |
| 23:40:13 | video_generate × 3（第二次重试，换 DashScope provider） | 部分成功 |
| 23:49:12 | 记录项目信息 | 交付 |

## 根因分析

### 触发链条

```
用户提供 Alex 参考图 (Pixar 风格 3D CGI)
    ↓
LLM 使用 reference2video 模式并行生成 7 个场景
    ↓
5/7 场景触发 AI 视频模型的版权审核机制
错误信息: "The request failed because the output video may be related to copyright restrictions"
    ↓
LLM 降级策略 1: 移除参考图，仅用文字描述角色
    ↓
2/5 成功，3/5 仍触发版权（prompt 中的描述仍被判定为版权风险）
    ↓
LLM 降级策略 2: 换用 DashScope/HappyHorse provider (text2video)
    ↓
最终成功生成所有场景，但角色一致性完全丧失
```

### 根本原因：LLM 的版权错误降级策略破坏了角色一致性

问题的核心不在于版权审核本身，而在于 **LLM 在遇到版权错误时的降级策略没有考虑角色一致性**：

| 降级层级 | 策略 | 角色一致性影响 |
|----------|------|--------------|
| 原始 | reference2video + 参考图 | 高一致性（图像驱动） |
| 降级 1 | 同 provider，移除参考图，纯文字描述 | 低一致性（文字无法精确控制外观） |
| 降级 2 | 换 provider (DashScope)，纯文字描述 | 极低一致性（不同模型风格差异大） |

**最终成片中的角色来源混杂**：

| 场景 | 生成方式 | Alex 外观 |
|------|---------|-----------|
| Scene 4 | 参考图 + 原始 provider | 与参考图一致（蓝衬衫、棕发、Pixar 风格） |
| Scene 2, 3 | 纯文字描述 + 原始 provider | 模型自由发挥，可能偏离参考 |
| Scene 1, 5, 6 | 纯文字描述 + DashScope | 完全不同风格，角色外观大幅偏离 |

### 版权触发的可能原因

参考图为 Pixar 风格 3D CGI 角色，AI 视频模型的安全审核可能将其判定为：
- 与 Pixar/Disney 已有角色过于相似
- 3D CGI 风格本身触发了"动画角色版权"的审核规则
- 特定场景的 prompt + 参考图组合被判定为试图复制受版权保护的内容

## 解决方案

### 方案一：版权失败时统一降级所有场景（推荐）

当任何场景因版权失败时，**所有场景都应使用相同的降级策略重新生成**，而非仅重试失败的场景：

```markdown
## Copyright Failure Handling Rules

1. 当 video_generate 返回 "copyright restrictions" 错误时：
   - 不要仅重试失败的场景
   - 将所有已成功的场景也标记为"需要重新生成"
   - 选择一个统一的降级策略应用到所有场景

2. 降级策略优先级：
   a. 修改参考图（降低与版权内容的相似度）后重试所有场景
   b. 如果参考图本身有版权风险，使用 image_generate 生成一张新的原创参考图
      然后用新参考图重新生成所有场景
   c. 如果必须放弃参考图，所有场景统一使用同一 provider + 相同的详细文字描述

3. 绝对禁止：
   - 混合使用"有参考图"和"无参考图"生成的场景
   - 混合使用不同 provider 生成的场景（除非经过一致性验证）
```

### 方案二：版权失败时先生成新的原创参考图

```markdown
## Copyright Recovery with New Reference Image

当参考图触发版权审核时：
1. 使用 image_generate 基于文字描述生成一张新的原创角色参考图
   - 保留角色的关键特征（服装颜色、发色、体型）
   - 但调整风格以避免版权风险（如从 Pixar 风格改为通用 3D 卡通风格）
2. 用新参考图对所有场景统一使用 reference2video 模式重新生成
3. 这样既保持了角色一致性，又规避了版权问题
```

### 方案三：前置版权风险检测

```markdown
## Pre-generation Copyright Risk Check

1. 在批量生成视频前，先用参考图生成 1 个测试场景
2. 如果测试场景触发版权，立即执行方案二（生成新参考图）
3. 不要在 5/7 场景失败后才开始降级处理
4. 这样可以避免浪费 API 调用额度和时间
```

### 方案四：角色一致性验证门控

```markdown
## Character Consistency Gate

在所有场景生成完成后、合成/交付前：
1. 对所有包含角色的场景调用 analyze_file_content 提取角色外观描述
2. 比较各场景中角色的关键特征（服装、发色、肤色、风格）
3. 如果一致性评分低于阈值：
   - 标记不一致的场景
   - 使用一致性最高的场景作为基准，重新生成不一致的场景
   - 或通知用户存在一致性问题，提供选项
```

## 影响评估

| 维度 | 评估 |
|------|------|
| 用户感知 | **极高** — 主角外观前后不一致是最明显的质量问题 |
| 发生频率 | 中 — 当参考图为知名动画风格时容易触发 |
| 修复成本 | 中 — 需要修改 LLM 的错误处理逻辑 |
| 推荐优先级 | **P0** |

## 总结

项目 42917497221 的角色不一致问题根因是：**LLM 在遇到版权审核失败时，采用了"逐步降级"策略（移除参考图 → 换 provider），但只对失败场景重试，未对已成功场景同步降级，导致最终成片中同一角色在不同场景使用了 3 种不同的生成方式，外观严重不一致。**

核心修复建议：当版权失败触发降级时，必须对所有场景统一应用相同的生成策略，确保角色一致性优先于单场景成功率。

## 补充讨论：为什么版权问题只在个别片段出现

### 疑问

同一个项目中所有场景都使用了同一张参考图，为什么版权审核不是全部触发或全部通过，而是只有部分场景被拦截？

### 解答

AI 视频模型的版权审核机制是针对**生成结果（输出）**而非**输入图片**进行检测的。具体原因如下：

**1. 审核对象是"最终生成物"而非"输入素材"**

版权检测不是在输入阶段拦截参考图，而是在模型生成视频后，对输出内容进行安全审核。同一张参考图搭配不同的 prompt（不同场景描述），会生成不同的视频内容，审核系统判定的是最终输出是否与受版权保护的内容相似。

**2. 不同 prompt 产生不同的输出风格**

例如 scene4（成功）的 prompt 可能是"Alex 在户外观察昆虫"，输出画面与任何已知版权内容不相似；而 scene1（失败）的 prompt 可能是"Alex 仰望星空表情惊奇"，输出画面恰好与某个 Pixar 电影场景过于相似。

**3. 审核具有概率性**

模型每次生成的具体帧画面不同，某些场景的生成结果恰好触发了相似度阈值，另一些则没有。即使是相同的 prompt 重复生成，也可能出现时而通过、时而失败的情况。

**4. 场景动作/构图的影响**

特定的角色姿态、表情、场景构图组合更容易与已知版权作品匹配。例如"小男孩仰望天空"这个构图在 Pixar/Disney 电影中非常常见，更容易触发版权检测。

**5. 佐证：移除参考图后仍有场景失败**

LLM 第一次重试时移除了参考图，仅用纯文字描述角色，但 scene1/5/6 仍然触发版权。这进一步证明版权检测不仅看输入图片，还看 prompt 描述 + 生成结果的组合。某些场景的文字描述本身就会引导模型生成与版权内容相似的画面。

### 类比理解

就像用同一张人脸照片 + 不同文字描述生成视频：有些描述生成的结果看起来像某部电影的片段（触发版权），有些则不像（通过审核）。审核的是"最终生成物"，不是"输入素材"。
