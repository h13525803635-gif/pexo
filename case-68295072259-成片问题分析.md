# Pexo 项目 68295072259 — 成片问题根因分析

## 问题描述

项目 68295072259 最终交付的视频 `mobile_app_mockup.mp4` 存在以下成片问题：
1. **无音频轨道** — 视频完全静音，无 BGM
2. **渲染 warnings 未修复** — GSAP 动画重叠、元素可见性控制缺失、根元素缺少 data-start

## 项目基本信息

| 字段 | 值 |
|------|------|
| 项目 ID | 68295072259 |
| Langfuse Trace ID | a45e8c7292da45f8403e1b36e3acf2ff |
| Session ID | thread_75127832422_68295072259_01KTT8HXTV689JVJ6MWBHMKMGY |
| 执行时间 | 2026-06-11T02:35:39 ~ 02:41:40 UTC（约 6 分钟） |
| 环境 | test |
| 使用模型 | global.anthropic.claude-sonnet-4-6 |
| 最终输出 | mobile_app_mockup.mp4 (asset_id: a_8BmnnFo, 渲染耗时 71s) |
| 总 observations | 163 |
| 工具调用数 | 29 |

## 用户原始请求

> "Scale down 03_brand/03_mobile_app.png and place it inside the screen area of a smartphone device frame to simulate a real device mockup. At 00:06, play a circular expanding click ripple animation in the middle of the screen. At 00:07, pop up a tooltip right above that position saying 'AI Recommendation: Based on your activity, today's goal is 8,000 steps'."

## LLM 执行计划

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1 | Read the hyperframes skill to plan the composition | completed |
| 2 | Analyze the mobile app screenshot to understand its dimensions and content | completed |
| 3 | Get available fonts for the composition | completed |
| 4 | Build HTML composition with phone mockup, ripple animation, and tooltip | completed |
| 5 | Render and deliver the final video | completed |

**注意**：计划中没有音频相关步骤，也没有"修复渲染 warnings"步骤。

## 问题一：无音频轨道

### 根因

与项目 00997593201 相同的根本原因：**用户请求中未提及音频/音乐/BGM，LLM 严格按字面意思执行，不主动添加音频。**

LLM 在渲染完成后直接调用 `show_final_video` 交付，没有经过 `probe_media` 验证视频流信息，也没有调用 `assembly-skill` 添加背景音乐。

### 影响

交付的视频完全静音，作为手机 App 展示视频缺乏氛围感和完整度。

## 问题二：渲染 Warnings 未修复

### submit_render 返回的 5 个 warnings

| 编号 | Warning 类型 | 具体内容 | 影响 |
|------|-------------|---------|------|
| 1 | overlapping_gsap_tweens | `#ring-1` 的 scale/opacity 在 6.00s-6.01s 重叠 | 动画可能出现闪烁或跳帧 |
| 2 | overlapping_gsap_tweens | `#ring-2` 的 scale/opacity 在 6.10s-6.11s 重叠 | 同上 |
| 3 | overlapping_gsap_tweens | `#ring-3` 的 scale/opacity 在 6.22s-6.23s 重叠 | 同上 |
| 4 | timed_element_missing_clip_class | `<div id="scene-main">` 有 timing 属性但无 `class="clip"` | 元素在整个视频中始终可见，而非仅在预定时间段内显示 |
| 5 | root_composition_missing_data_start | 根元素 `"mobile-app-mockup-v1"` 缺少 `data-start` | 播放起始时间不确定，可能影响时间线对齐 |

### 根因

**LLM 收到 submit_render 的 warnings 后没有修复就继续等待渲染完成。** 具体流程：

1. `02:38:25` — LLM 调用 `submit_render`，返回 `success: true` + 5 个 warnings
2. `02:38:28` — LLM 直接调用 `sleep(15)` 开始等待渲染
3. `02:38:47 ~ 02:41:22` — 持续轮询 `query_render` 直到渲染完成
4. `02:41:25` — 标记所有 todos 为 completed
5. `02:41:34` — 直接 `show_final_video` 交付

**LLM 将 `success: true` 理解为"可以继续"，忽略了 warnings 中的质量问题。** 特别是 warning 4（`timed_element_missing_clip_class`）和 warning 5（`root_composition_missing_data_start`）可能导致视频时间线行为异常。

### 影响

- 3 个 GSAP 重叠 warnings：ripple 动画的三个环在开始时可能有微小的视觉跳动（影响较小，因为重叠仅 0.01s）
- `scene-main` 缺少 clip class：主场景在整个视频中始终可见，无法按时间段控制显示/隐藏
- 根元素缺少 `data-start="0"`：HyperFrames 运行时可能无法正确确定播放起始点

## 解决方案

### 问题一（无音频）解决方案

同项目 00997593201 的建议：在系统提示词中增加默认音频规则。

```
## Audio Default Rule
Unless the user explicitly requests a silent video or says "no audio/music/BGM",
ALL delivered videos MUST include at least one audio track (BGM).
After rendering is complete and before calling show_final_video:
1. Generate or select appropriate BGM using music_generate or assembly-skill
2. Mix the audio into the video using assembly-skill
3. Verify the final video has an audio stream via probe_media
```

### 问题二（Warnings 未修复）解决方案

#### 短期方案：修改 LLM 行为规则

在 hyperframes-skill 的 SKILL.md 中增加 warning 处理规则：

```
## Warning Handling After submit_render

When submit_render returns warnings:
1. CRITICAL warnings (missing_clip_class, missing_data_start): MUST fix before proceeding
   - Cancel or wait for current render
   - Edit the HTML to fix the issues
   - Re-submit render
2. NON-CRITICAL warnings (overlapping_gsap_tweens with < 0.1s overlap): 
   - Log but may proceed if overlap is negligible
   - Fix if time permits

NEVER ignore warnings with [timed_element_missing_clip_class] or 
[root_composition_missing_data_start] — these affect playback correctness.
```

#### 中期方案：渲染管线拦截

在 `submit_render` 工具中增加 warning 严重级别分类：
- `error` 级别：阻止渲染，强制 LLM 修复
- `warning` 级别：允许渲染但在 `query_render` 完成时再次提醒
- `info` 级别：仅记录

#### 长期方案：自动修复

在 HyperFrames 渲染管线中增加自动修复能力：
- 自动为缺少 `data-start` 的根元素添加 `data-start="0"`
- 自动为有 timing 属性但缺少 `clip` class 的元素添加 `class="clip"`
- 自动为重叠的 GSAP tweens 添加 `overwrite: "auto"`

## 执行时间线

| 时间 (UTC) | 操作 | 说明 |
|-----------|------|------|
| 02:35:39 | write_todos | 制定 5 步计划 |
| 02:35:45 | read_file + analyze_file_content + list_fonts | 并行读取 skill、分析图片、获取字体 |
| 02:36:02 | read_file (SKILL.md 续) | 读取 skill 后半部分 |
| 02:36:07 | read_file × 2 + get_file_info | 读取 composition 规则和骨架模板 |
| 02:36:25 | write_file | 编写 HTML 合成文件 |
| 02:37:41 | render_frame × 3 | 预览 3 个关键帧 |
| 02:38:10 | add_attachments | 发送预览图给用户 |
| 02:38:25 | **submit_render** | **提交渲染，收到 5 个 warnings（未修复）** |
| 02:38:28–02:41:22 | sleep + query_render × 5 | 等待渲染完成 |
| 02:41:25 | write_todos | 标记所有步骤完成 |
| 02:41:34 | **show_final_video** | **直接交付（未验证、未添加音频）** |

## 总结

| 维度 | 问题一（无音频） | 问题二（Warnings 未修复） |
|------|----------------|------------------------|
| 根因 | 系统无默认音频规则 | LLM 将 `success:true` 视为可继续，忽略 warnings |
| 影响 | 视频静音，体验不完整 | 可能导致动画闪烁和时间线异常 |
| 修复优先级 | 高 | 中（当前 warnings 实际影响较小） |
| 建议方案 | 系统级默认音频规则 | 在 skill 中增加 warning 分级处理规则 |
