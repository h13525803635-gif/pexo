# Pexo 项目 00997593201 — 视频无音轨根因分析

## 问题描述

项目 00997593201 最终交付的视频 `cafe_rating_final.mp4` 没有音频轨道（audio stream）。

## 项目基本信息

| 字段 | 值 |
|------|------|
| 项目 ID | 00997593201 |
| Langfuse Trace ID | f9c1f36bd61d2e14cd57896f087fb532 |
| Session ID | thread_75127832422_00997593201_01KTT8KPSF5PM3K820JWW0CBKA |
| 执行时间 | 2026-06-11T02:36:36 ~ 05:14:47 UTC |
| 环境 | test |
| 使用模型 | global.anthropic.claude-sonnet-4-6 |
| 最终输出 | cafe_rating_final.mp4 (17.2MB, 720×1280, 30fps, 15s, h264, 无音频) |

## 用户原始请求

> "Generate a slow-motion video of a cafe interior using 03_brand/05_cafe_interior.png. Overlay a warm-toned circular bokeh light effect (screen blend mode) throughout the video. At 00:08, pop up a rating card in the exact center with an elastic scale animation: 'Atmosphere ⭐4.8 | Coffee ⭐4.9 | Service ⭐4.7 | Overall 9.5/10'. Keep it for 4 seconds, then scale down to disappear."

## 根因分析

### 直接原因

LLM 在最终交付前的验证步骤中（Generation at 05:14:27 UTC）明确判断：

> "15s, 720×1280, 30fps — perfect. **Audio is `explicit_silent` as intended (no audio was requested).**"

LLM 认为用户没有在请求中提及音频/音乐/BGM，因此将视频标记为"故意静音"（explicit_silent），直接交付了无音频的视频。

### 根本原因

**系统缺乏"默认添加背景音乐"的规则。** 具体表现为：

1. **用户请求中确实没有提到音频**：用户的 prompt 只描述了视觉效果（慢动作、散景光效、评分卡动画），没有任何关于音频、音乐或音效的描述。

2. **LLM 的任务计划中没有音频步骤**：LLM 制定的 4 步计划为：
   - ✅ Analyze the cafe interior image and read creative guidelines
   - ✅ Generate slow-motion cafe interior video
   - ✅ Build HyperFrames composition with bokeh overlay and rating card animation
   - ✅ Render and deliver the final video

   计划中没有"添加背景音乐"或"音频后期处理"步骤。

3. **系统提示词中没有强制音频规则**：虽然系统中有 `assembly-skill`（支持 audio production / VO/TTS / BGM / audio mixing），也有 `music_generate` 和 `audio_produce` 工具，但系统提示词中没有规定"所有视频必须包含音频轨道"或"默认为视频添加 BGM"。

4. **HyperFrames 渲染管线不自动添加音频**：`submit_render` 工具渲染 HTML 动画时只产生视频流，不会自动附加音频。需要额外调用 `assembly-skill` 来合成音频。

### 执行流程概要

| 阶段 | 时间 (UTC) | 操作 | 说明 |
|------|-----------|------|------|
| 1 | 02:36 | 分析图片 + 读取 skill | 读取 generation-skill 和 hyperframes-skill |
| 2 | 02:37–02:43 | 编写 HTML 合成 | 多次 write_file/edit_file 构建动画 |
| 3 | 02:43–03:19 | 第一次渲染 | submit_render → 长时间轮询 query_render（约 30 分钟） |
| 4 | 03:19–04:07 | 第二次渲染 | 渲染失败后重试，继续轮询（约 50 分钟） |
| 5 | 04:07–04:48 | 第三次渲染 | 再次重试 |
| 6 | 04:48–05:14 | 最终渲染成功 | 第四次 submit_render 成功 |
| 7 | 05:14 | 验证并交付 | probe_media 确认视频参数，**跳过音频**，直接 show_final_video |

**关键缺失步骤**：在步骤 6 和 7 之间，应该有一个"调用 assembly-skill 添加 BGM"的步骤，但 LLM 没有执行。

## 解决方案

### 短期方案（Prompt 层面）

在系统提示词或 skill 规则中增加以下规则：

```
## Audio Default Rule
Unless the user explicitly requests a silent video or says "no audio/music/BGM",
ALL delivered videos MUST include at least one audio track (BGM).
After rendering is complete and before calling show_final_video:
1. Generate or select appropriate BGM using music_generate or assembly-skill
2. Mix the audio into the video using assembly-skill
3. Verify the final video has an audio stream via probe_media
```

### 中期方案（Skill 路由层面）

1. **在 hyperframes-skill 的 SKILL.md 中添加音频提醒**：在"渲染完成后"的步骤中，增加"检查是否需要添加音频"的指引。

2. **在 assembly-skill 的触发条件中增加**：当 HyperFrames 渲染完成后，自动触发 assembly-skill 进行音频合成。

3. **在 write_todos 的模板中默认包含音频步骤**：确保 LLM 在制定计划时不会遗漏音频。

### 长期方案（系统架构层面）

1. **渲染管线自动音频注入**：在 `submit_render` 或 `show_final_video` 的后处理中，自动检测视频是否有音频轨道。如果没有且用户未明确要求静音，则自动触发 BGM 生成和混合。

2. **交付前质量检查 Gate**：在 `show_final_video` 工具中增加前置检查，如果视频没有音频轨道，返回警告信息给 LLM，提示其先添加音频。

3. **用户偏好系统**：在用户 preferences 中支持设置"默认添加 BGM = true/false"，让系统根据用户偏好自动决定。

## 总结

| 维度 | 说明 |
|------|------|
| 根因 | LLM 严格按照用户请求执行，用户未提及音频则不添加 |
| 缺失 | 系统无"默认添加 BGM"规则，HyperFrames 管线不自动附加音频 |
| 影响 | 交付视频无音轨，用户体验不完整 |
| 修复优先级 | 高 — 添加系统级默认音频规则即可解决 |
