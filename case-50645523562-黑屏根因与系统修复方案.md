# Pexo 项目 50645523562 黑屏根因与系统修复方案

## 1. 故障现象与用户反馈

用户反馈项目 50645523562（Tutti 30 秒竖版广告）的成片存在严重问题：“**前面13秒和后6秒都没内容**”。

通过对系统生成的 `composition.html` 源码、lint 修复记录以及 QC 帧进行深入追踪分析，确认用户的反馈完全属实。黑屏并非由于视频素材过暗导致，而是由代码结构和框架机制冲突引起的彻底不可见。

---

## 2. 根因分析（按时段拆解）

### 2.1 前 13 秒黑屏原因

前 13 秒实际上由三段不同的错误叠加而成：

| 时段 | 元素 | 根因 |
|------|------|------|
| **0–8s** | `v-gu01`（8秒视频） | **`data-start=0` 触发 clip 框架边界条件 bug**。<br>该视频没有任何 GSAP 动画代码，完全依赖 clip 框架的 `data-start=0` 属性来控制显示。但在当前渲染器版本中，`t=0` 时 clip 框架自身正在初始化，导致 `data-start=0` 的元素被跳过，其 CSS `visibility` 始终保持为 `hidden`。 |
| **8–10s** | 无元素 | **设计间隙**。<br>时间线设计上，`v-gu01` 在 8s 结束，而下一个场景 `scene-b3` 在 10s 才开始。这 2 秒内没有任何前景元素，只显示深棕色背景。 |
| **10–13.5s** | `scene-b3`（奶油色卡片） | **GSAP 与 clip 框架控制权冲突**。<br>`scene-b3` 是一个带有 `.clip` 类的元素。clip 框架通过 `visibility` 属性管理其显隐。但 GSAP 脚本中使用了 `gsap.set("#scene-b3", { opacity: 0 })` 将其初始化为透明。渲染器在 `t=10s` 时将其设为 `visibility:visible`，但 GSAP 的 `opacity:0` 使其仍然不可见。渲染器在处理 `.clip` 元素的 GSAP 动画时存在局限，导致 `tl.to opacity:1` 动画未能正确触发。 |

### 2.2 后 6 秒黑屏原因

| 时段 | 元素 | 根因 |
|------|------|------|
| **24–28s** | `scene-b5`（步骤说明卡片） | **GSAP 与 clip 框架控制权冲突**。<br>与 `scene-b3` 完全相同，带有 `.clip` 类且被 `gsap.set(opacity:0)` 初始化，导致始终透明。 |
| **28–30s** | `scene-b6`（绿色 CTA 卡片） | **GSAP 与 clip 框架控制权冲突**。<br>同上。 |

### 2.3 为什么 AI 没能修复？

1. **错误的 HTML 结构回退**：在 lint 修复阶段，lint 工具正确指出了 `GSAP_ANIMATES_CLIP_ELEMENT` 错误。AI 曾尝试将 `id="scene-b3"` 移到内层非 `.clip` 的 wrapper div 上（这是正确的修复方式），但在后续处理其他元素的 lint 错误时，AI 读取了部分代码片段，误将 HTML 结构又回退到了错误状态。
2. **对 QC 结果的错误归因**：AI 在提交渲染前的 QC 阶段，发现除了 15s 之外的所有帧都是黑屏。但 AI 错误地将此归因为“`render_frame` 工具不支持 GSAP 动画播放的局限性”，而没有意识到这是 `composition.html` 自身的严重 bug。

---

## 3. Pexo 系统级防范方案

为了避免同类问题在未来的项目中再次发生，需要在 Pexo 的工具链、规则库和 AI 认知层面进行系统性加固。

### 3.1 针对 GSAP 操控 `.clip` 元素冲突的防范

1. **Lint 规则加强**
   - 当前的 `GSAP_ANIMATES_CLIP_ELEMENT` 规则仅检测 `visibility`。需要扩展该规则，使其同时检测针对 `.clip` 根元素的 `gsap.set({ opacity: 0 })` 和 `tl.to({ opacity: ... })` 操作。
   - 若检测到针对 `.clip` 元素的 opacity 操控，直接判定为 `error` 级别，强制 AI 必须修复。

2. **文档规范 (`tech-html.md`) 补充**
   - 在第 10 节（Banned in `<script>` / GSAP）中明确加入：禁止写 `gsap.set(".clip-element", { opacity: 0 })`。
   - 给出明确的正确范例：如果需要用 GSAP 控制透明度，必须将 id 移到内层的非 `.clip` wrapper 上。

3. **Motion Skill 模板更新**
   - 在 AI 生成 DOM scene 的标准模板中，默认采用“内层 wrapper”模式，确保外层 `.clip` 元素不带 `id`。

### 3.2 针对 `data-start=0` 元素不显示的防范

1. **渲染引擎修复（治本）**
   - 修复 clip 框架在初始化时的时序问题，确保 `data-start=0` 的元素在 `t=0` 时能被正确设为 `visibility: visible`。

2. **Lint 规则新增（防范）**
   - 增加警告规则：若发现 `data-start=0` 的 `.clip` 元素且该元素没有任何绑定的 GSAP 动画，发出 `CLIP_ZERO_START_NO_GSAP` 警告，提示 AI 将其改为 `data-start=0.1` 或添加 GSAP 保护性淡入动画。

### 3.3 针对 AI 在 QC 阶段误判的防范

1. **QC 认知规则修正**
   - 在 motion-skill 的 QC 规则中明确指出：如果 QC 帧出现黑屏，且该时段的元素使用了 `gsap.set(opacity:0)`，**严禁**将其归因为 `render_frame` 的工具局限性，必须立刻检查该元素是否违规操控了 `.clip` 根元素。

2. **强制阻断机制**
   - 在 `submit_render` 的前置检查（preflight）中加入逻辑：如果 QC 抽帧的黑屏比例超过 50%，系统直接拒绝提交渲染，强制要求 AI 重新排查 `composition.html` 的结构并修复。
