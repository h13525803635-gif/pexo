# Pexo 项目 49169912420 & 74524136316 — 音频重叠问题根因分析

## 问题描述

项目 49169912420 和 74524136316 均出现了 **BGM 与视频素材原声同时播放** 导致的音频重叠问题。两个项目属于同一时间段（2026-06-17 06:31~06:58 UTC）、同一用户，问题模式完全一致。

## 项目基本信息

| 字段 | 项目 49169912420 | 项目 74524136316 |
|------|-----------------|-----------------|
| Session ID | thread_75127832422_49169912420_01KVA4BCV735FS27AWJMR4V43T | thread_75127832422_74524136316_01KVA4E16AW5ZES95C5AP9NKK8 |
| 环境 | test | test |
| Trace 数量 | 3 | 4 |
| 创建时间 | 2026-06-17T06:37:29 UTC | 2026-06-17T06:31:25 UTC |
| 视频类型 | 旅行广告（Meridian Travel Co. 30s 竖版商业广告） | 同类型商业广告 |
| 音频来源 | ElevenLabs music_v1 生成 BGM + video_generate 生成的视频自带音频 | 同上 |

## 根因分析

### 直接原因

HyperFrames 渲染器在合成最终视频时，**同时混合了两路音频**：
1. 独立 BGM 音轨（`<audio>` 元素，通过 ElevenLabs 生成）
2. 视频素材自带的音频轨道（`<video>` 元素未设置 `muted`）

### 技术细节（以 49169912420 为例）

**音频生成阶段**：

LLM 通过 `music_generate` 工具生成了 BGM：
```
music_generate | 2026-06-17T06:41:31
  provider: elevenlabs
  model: music_v1
  mode: text2music
  prompt: "Uplifting cinematic travel commercial music. Warm orchestral with light acoustic guitar..."
  → 输出: meridian_bgm_20260617T064138_a03b7d0f.mp3 (asset_id: a_T4T3v3o)
```

**HTML 合成阶段**：

LLM 在 HTML 中同时嵌入了：
- BGM 作为 `<audio>` 元素（Track 1，全时长 0~32.2s）
- 视频素材作为 `<video>` 元素（Track 0），且设置了 `data-has-audio="true"`

```html
<!-- BGM 音轨 -->
<audio id="bgm" class="clip" src="asset://a_T4T3v3o" 
       data-start="0" data-duration="32.2" data-track-index="1"></audio>

<!-- 视频素材（自带音频） -->
<video id="vid-s1" class="bg" src="asset://a_oS4f8o6" 
       data-has-audio="true" data-start="0" data-duration="15.07" 
       data-track-index="0"></video>
```

**Lint 修复阶段**：

第一次 `submit_render` 返回 lint 错误：
> `[video_muted_with_declared_audio]` — `<video>` declares `data-has-audio="true"` but also has `muted`. Studio preview will silence the video audio.

LLM 为修复此 lint 错误，**移除了 `muted` 属性**，使视频原声被启用。但此时 HTML 中已经有独立的 BGM `<audio>` 元素，两路音频同时输出。

### 根本原因

**系统缺乏音频互斥规则。** 具体表现为：

1. **hyperframes-skill 未定义音频优先级策略** — 当同时存在独立音频轨道（BGM/VO）和视频内嵌音频时，应该如何处理没有明确规则
2. **Lint 规则 `[video_muted_with_declared_audio]` 的修复方向不明确** — 该 lint 只告诉 LLM "muted 和 data-has-audio 冲突"，但没有指导在有独立音频轨道时应该选择哪个方向修复
3. **LLM 默认选择了"启用视频音频"** — 移除 muted 而非移除 data-has-audio，导致两路音频同时播放
4. **渲染前无音频冲突检测** — submit_render 的 lint 不检查"同一时间段是否有多路音频重叠"

## 问题影响

- 最终成片中 BGM 和视频原声同时播放，音频混乱
- 用户体验极差，需要重新制作
- 两个项目均受影响，说明这是系统性问题而非偶发

## 解决方案

### 方案一（推荐）：在 hyperframes-skill 中增加音频互斥规则

在 skill 文档中明确添加以下规则：

```markdown
## Audio Exclusivity Rules (MUST follow)

### Rule 1: BGM/VO 优先于视频内嵌音频
当合成中存在独立音频轨道（<audio> 元素，如 BGM 或 voiceover）时：
- 所有 <video> 元素 MUST 添加 `muted` 属性
- 所有 <video> 元素 MUST NOT 声明 `data-has-audio="true"`

### Rule 2: 修复 [video_muted_with_declared_audio] lint 的正确方向
- 如果 HTML 中存在独立 <audio> 元素 → 移除 `data-has-audio`（保留 muted）
- 如果 HTML 中不存在独立 <audio> 元素 → 移除 `muted`（保留 data-has-audio）

### Rule 3: 渲染前音频检查
在 submit_render 之前，MUST 验证：
- 同一时间段内不得同时存在 unmuted <video> 和 <audio> 元素
- 如果发现冲突，优先保留独立音频轨道，将视频设为 muted
```

### 方案二：增强 Lint 规则

在 HyperFrames 渲染器的 lint 阶段增加新的检查规则：

```
[audio_overlap_detected] — Unmuted <video> and <audio> elements overlap 
in time range [X-Y]. This will cause audio mixing. Either mute the video 
or remove the <audio> element.
```

该规则应为 **ERROR 级别**（阻止渲染），而非 WARNING。

### 方案三：video_generate 默认行为调整

在 `video_generate` 工具生成视频时，如果项目计划中已包含独立音频（BGM/VO），则：
- 生成的视频素材默认不包含音频轨道
- 或在返回结果中标注 `has_audio: true`，提醒 LLM 在合成时需要处理

## 时间线（以 49169912420 为例）

| 时间 (UTC) | 事件 |
|-----------|------|
| 06:37:29 | 项目创建 |
| 06:41:31 | music_generate 生成 BGM (meridian_bgm) |
| 06:47:49 | media_probe 验证 BGM 文件 |
| 06:49:14 | write_file 创建 HTML，BGM 作为 Track 1 |
| 06:50:03 | edit_file 将 BGM `<audio>` 元素插入 HTML |
| 06:52:15 | submit_render #1 失败 — lint 报错 video_muted_with_declared_audio |
| 06:52:50 | edit_file 修复 vid-s1：移除 muted（错误方向） |
| 06:52:56 | edit_file 修复 vid-s2：移除 muted（错误方向） |
| 06:54:21 | submit_render 成功（lint 通过，但音频已重叠） |
| 06:55:27 | show_final_video 交付（音频重叠的视频） |

## 总结

| 维度 | 说明 |
|------|------|
| 问题类型 | BGM 与视频内嵌音频同时播放 |
| 根因 | 系统缺乏音频互斥规则 + LLM 修复 lint 时选错方向 |
| 影响范围 | 所有使用 BGM + 带音频视频素材的 HyperFrames 合成项目 |
| 优先级 | **P0** — 直接影响成片质量 |
| 推荐方案 | 在 hyperframes-skill 中增加音频互斥规则 + 增强 lint 检测 |
