# Case 52984367840: 指定五段视频后仍混入其他素材

## 问题概述

用户在最终合成阶段明确附带了五段视频，并要求：

> 基于这五段素材，组合一个完整的《Spider-Man: Brand New Day》短片，不要有割裂感。

最终交付的 `spiderman_bnd_final.mp4` 却出现了三个问题：

1. 时间线包含六段视频，其中一段不是用户本轮指定的素材。
2. 用户指定的五段素材虽然均被使用，但多段被裁短，没有完整播放。
3. 各视频自带的拼贴动画音效全部消失，只剩 VO 和 BGM。

## 用户指定的素材白名单

用户本轮明确附带的五段视频为：

| 顺序 | 素材 |
| --- | --- |
| 1 | `collage_scene1_shatter_20260714T045800_f63aa8a0.mp4` |
| 2 | `collage_scene2_city_20260714T045747_ed986396.mp4` |
| 3 | `collage_scene4_spider_v1_20260714T084524_fdd9f354.mp4` |
| 4 | `collage_scene4_boy_v1_20260714T085006_6c0a85d2.mp4` |
| 5 | `collage_scene5_title_v2_20260714T053812_6505e06a.mp4` |

这五项应当构成最终合成的封闭输入集合。除非用户确认，Agent 不应从项目历史或会话摘要中补入其他视频。

## 实际时间线

最终 `composition.html` 声明了 `VIDEO SEQUENCE - 6 clips`，实际顺序为：

| 顺序 | 最终使用的中间素材 | 来源 | 时长 |
| --- | --- | --- | ---: |
| 1 | `scene1_cut.mp4` | 用户指定 scene1 | 6.916667s |
| 2 | `scene2_cut.mp4` | 用户指定 scene2 | 6.916667s |
| 3 | `scene3_cut.mp4` | **非本轮指定**的 `collage_scene3_mask_v2` | 6.916667s |
| 4 | `scene4_spider_cut.mp4` | 用户指定 spider | 3.583333s |
| 5 | `scene4_boy_cut.mp4` | 用户指定 boy | 3.583333s |
| 6 | `scene5_trim.mp4` | 用户指定 scene5 | 7.083333s |

额外素材 `collage_scene3_mask_v2` 对应 `asset://a_sQbeiAJ`。它不在用户本轮的五个附件中，却被 Agent 主动加入并裁为 `scene3_cut.mp4`。

## 根因

### 1. 当轮附件没有被视为严格白名单

Agent 受到此前“五场景故事结构”和会话摘要影响，将用户附带的两个 scene4 视频理解为“同一场景的两个半段”，而不是五个独立、完整的合成输入。

为了恢复其认知中的 scene1 到 scene5 结构，Agent 从历史资产中自行补入 `collage_scene3_mask_v2`。其任务列表已经直接暴露这一错误前提：

> 设计时间轴（6段 -> 如何裁剪到 32.52s）+ 规划过渡

也就是说，Agent 在写时间线之前已经决定使用六段，没有先验证这六段是否与用户本轮选择一致。

### 2. 先锁定 32.52 秒 VO，再反向裁剪视频

最终成片以 32.522 秒 VO 为主时钟，并增加约 0.5 秒 BGM 收尾。为了把六段素材和五个 0.4 秒 crossfade 塞进约 33 秒，Agent 执行了以下裁剪：

- scene1、scene2、额外 scene3：各裁到约 6.8 秒。
- spider、boy：各只保留约 3.5 秒。
- scene5：保留约 7.08 秒。

因此，用户指定的五段素材确实都被引用，但没有完整播放。问题不是素材解析失败，而是错误的六段时间线导致了不必要的裁剪。

### 3. 合成策略主动静音了所有视频原声

最终 HTML 中六个 `<video>` 元素全部设置了 `muted`，并明确记录：

```text
All video native audio -> muted
policy: mute_for_independent_audio
```

最终只保留：

- Liam VO，目标约 -16 LUFS。
- `bnd_bgm_bed`，`data-volume="0.12"`。

因此，各段生成视频中已有的纸张飞入、碰撞、whoosh、slam 等动作音效被整体删除。这是错误的音频策略选择，不是渲染器丢失音轨。

## 解决方案

### 1. 建立“当前附件优先”的素材白名单

当用户说“基于这 N 段素材合成”并在当前消息中附带 N 个视频时：

1. 按附件顺序生成 `selected_asset_ids`。
2. 最终视频轨只能引用该集合中的素材或由其直接派生的 trim/transcode 文件。
3. 禁止从历史素材、项目目录或会话摘要中自动补段。
4. 若叙事结构与附件数量冲突，必须以当前用户选择为准；确需补段时先征得用户确认。

建议在渲染前执行集合校验：

```text
source_assets(final_timeline) - selected_asset_ids == empty
count(logical_input_clips) == count(user_selected_clips)
```

派生文件必须保留 `derived_from_asset_id`，否则无法可靠完成该检查。

### 2. 将“素材数量”和“剧情场景数量”解耦

两个视频属于同一剧情场景，不代表可以额外补入另一个场景。时间线规划应使用用户选择的五个 clip，而不是强制恢复 scene1 到 scene5 的编号连续性。

本案例正确的视频轨应为：

```text
scene1 -> scene2 -> scene4_spider -> scene4_boy -> scene5
```

### 3. 裁剪前先做时长冲突决策

如果五段完整时长与 32.52 秒 VO 不匹配，应按以下顺序处理：

1. 优先减少转场重叠或调整 VO 后的尾帧。
2. 在不损害内容的前提下等比例、轻量裁剪五段素材。
3. 若需要大幅裁剪，例如把 7 秒素材砍到 3.5 秒，应先提示用户时长取舍。
4. 不得通过加入额外素材后再大幅裁剪用户指定素材来满足时长。

渲染前应输出可审计的时间线清单，包括原始时长、使用区间、裁剪比例和转场时长。

### 4. 原生 SFX 与独立 VO/BGM 分轨处理

不能因为存在独立 VO 和 BGM 就默认静音全部视频。正确策略为：

- 对每段视频探测原生音轨。
- 若原声包含对白且与 VO 重复，移除对白或静音该窗口。
- 若原声是与动作同步的 SFX，保留并在 VO 下方 duck。
- BGM 作为最低优先级音乐床，不应覆盖动作音效。

推荐混音优先级：

```text
P1: VO
P2: 关键动作 SFX
P3: 环境声
P4: BGM
```

渲染前增加音频断言：用户未要求静音且输入视频包含音轨时，最终混音中必须存在对应的 native-SFX 来源或明确记录丢弃原因。

## 验收标准

- 最终视频轨只包含用户本轮指定的五段素材及其直接派生文件。
- 不再出现 `collage_scene3_mask_v2` 或其他历史素材。
- 五段素材的裁剪区间透明可查；大幅裁剪必须经用户确认。
- 拼贴动作原生 SFX 可听见，VO 清晰，BGM 不遮盖 VO 与关键音效。
- 最终交付说明中的段数、时间线和实际 HTML 完全一致。

## 结论

本问题由三个连续决策错误造成：Agent 让历史剧情结构覆盖了当前附件白名单，错误地将五段扩成六段；随后为匹配固定 VO 时长裁短用户素材；最后又用 `mute_for_independent_audio` 移除了所有原生动作音效。

修复重点不是素材检索，而是建立当前轮附件的强约束、渲染前的素材集合校验，以及按内容类型处理原生音轨。
