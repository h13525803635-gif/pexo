# case-12996128437 — 链路、字幕、参考图、概念流程与仓库

本文档整理自当次对话结论，便于在本地 `skill` 文件夹中留存，**不替代**各 case 的详细 trace 文件（见 `analysis/langfuse-data/cases/12996128437/`）。

---

## 1. 项目 `12996128437`（Langfuse 真实链路摘要）

- **数据位置**：`analysis/langfuse-data/cases/12996128437/`（`trace-1-3accd5f5.json`、`trace-2-0959033b.json` 等）。
- **生图模型**：`image_generate` 使用 **Seedream**，模型 ID **`seedream-4-5-251128`**。
- **生视频**：`video_generate` 为 **Kling** `kling-v3-omni`，`reference2video`；每段 `image_list` 为 3 张 `file: /workspace/assets/...`。
- **「八张图 / 参考不全」**：Trace-1 三张概念 `style_frame_*` **未进入**任何 `video_generate` 的 `image_list`；Trace-2 五张生产 ref（如 `style_ref_anchor`、`char_ref_trio`、办公室/UI）进入各段。观感上易与「只用到部分图」混淆。
- **字幕**：TTS 返回含 `subtitle_file`；当次 **`execute_edit_video` 的 `edit_spec` 仅有视频轨 + 两条音频轨**，未见字幕轨；字幕异常需区分「前端 JSON 时间轴」与「画内模型乱字」。
- **画面「叠加」**：成片含 **clip 间 crossfade** 与 **VO + BGM + 段内 co-gen SFX**，属编排结果；非 trace 中额外神秘视频轨。

---

## 2. 概念图 vs 视频参考图；能否「脚本第一张当概念图」

- 常见流程是 **创意阶段概念/风格帧** 与 **成片阶段生产参考图** 职责不同；**并非物理上不能**复用同一张图。
- 「第一张分镜」未必在概念确认前已存在；概念图常偏 **风格/气质**，首段 key 偏 **具体叙事**。
- 实现上可改进为：用户确认后的概念帧 **升格并写入** 后续 `video_generate` 的 `image_list`，避免重复生图却丢掉概念帧。

---

## 3. GitHub `h13525803635-gif/pexo`

- 本地已在 **`/Users/luchen/Desktop/skill`** 执行 `git init`、提交、**`origin` 指向** `https://github.com/h13525803635-gif/pexo.git`。
- **推送需在您本机完成认证**（HTTPS 用户名/token 或 SSH）；自动化环境无法代填 GitHub 凭据。

---

## 4. 关于「桌面上的 skill」副本

- 曾将整库 **同步复制** 至 Finder **桌面** 下的文件夹：  
  **`/Users/luchen/Desktop/Desktop/skill`**（`file:///Users/luchen/Desktop/Desktop/skill/`）。
- 您本次说明 **无需撤回**上述复制；若以后不需要该副本，可在 Finder 中自行删除 **`桌面/skill`** 即可。
- **原工作区路径**（Cursor 常用）：**`/Users/luchen/Desktop/skill`** — 本纪要即写在此目录。

---

*文档由助手根据对话整理生成。*
