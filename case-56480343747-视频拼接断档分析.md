# 项目 56480343747 — 第一段与第二段拼接明显断档

## 问题描述

项目 56480343747 的成片中，第一段（A 段）和第二段（B 段）之间存在明显的视觉断档，画面内容、色调、运动方向在拼接处突然切换，缺乏连贯性。

## Langfuse 追踪关键信息

### 项目结构

该项目采用**分段渲染 + 拼接**的架构，共 4 个 HyperFrames 渲染段：

| 渲染段 | 输出名称 | 使用的 AI 视频素材 | 视频裁剪范围 |
|--------|---------|-------------------|-------------|
| seg_A1 | KEY_seg_A1_v2 | KEY_concert_S1 (a_vmKLk5Z) | 0 ~ 7.5s |
| seg_A2 | KEY_seg_A2_v2 | KEY_concert_S1 (a_vmKLk5Z) | 7.5 ~ 15.0s |
| seg_B1 | KEY_seg_B1_v2 | KEY_concert_S2 (a_Dqz3CM3) | 0 ~ 7.5s |
| seg_B2 | KEY_seg_B2_v2 | KEY_concert_S2 (a_Dqz3CM3) | 7.5 ~ 15.0s |

### AI 视频生成

两个视频素材均使用 `seedance` provider 的 `reference2video` 模式生成，各 15 秒：

- **KEY_concert_S1**：prompt 描述为暗黑环境中电光蓝色裂痕撕裂虚空，冲击波向外扩散
- **KEY_concert_S2**：prompt 描述为三个巨大的抽象字母形态从深空中显现，由光线和金属碎片构成

### 最终拼接顺序

```
seg_A1 (S1 前半) → seg_A2 (S1 后半) → seg_B1 (S2 前半) → seg_B2 (S2 后半)
```

## 根因分析

### 断档发生位置

断档发生在 **seg_A2 → seg_B1** 的拼接处，即成片的约第 15 秒：

```
seg_A2 最后一帧: KEY_concert_S1 视频的第 15 秒画面
                    ↓ 硬切（无过渡）
seg_B1 第一帧:   KEY_concert_S2 视频的第 0 秒画面
```

### 根本原因：两个独立 AI 视频之间缺乏视觉连续性设计

| 因素 | 说明 |
|------|------|
| **独立生成** | S1 和 S2 是两次独立的 video_generate 调用，模型无法感知另一个视频的内容 |
| **prompt 不连贯** | S1 描述"电光裂痕撕裂虚空"，S2 描述"字母形态从深空显现"，两者视觉语言完全不同 |
| **无过渡处理** | 4 段直接硬拼接，seg_A2 → seg_B1 之间没有 crossfade、dissolve 或任何转场效果 |
| **分段渲染的固有缺陷** | HyperFrames 分段渲染后直接拼接，渲染器不会自动添加段间过渡 |

### 为什么 A1→A2 不断档但 A2→B1 断档

- **A1→A2**：使用的是同一个视频（S1）的前后两半，画面天然连贯
- **A2→B1**：使用的是两个完全不同的 AI 视频（S1 尾帧 vs S2 首帧），画面必然不连贯

## 解决方案

### 方案一：在分段拼接处添加转场效果（推荐，最小改动）

```markdown
## Segment Transition Rules

当成片由多个分段渲染拼接而成时：
1. 如果相邻段使用不同的视频素材，必须在拼接处添加转场效果：
   - 优先使用 crossfade（0.5~1.0s）
   - 或使用 dissolve、fade-through-black 等
2. 可通过 media_process 的 concat 操作指定 transition 参数
3. 如果相邻段使用同一视频素材的连续片段，可以硬切
```

### 方案二：确保相邻段的视频素材视觉连贯

```markdown
## Cross-segment Visual Continuity

当需要多个 AI 视频素材拼接时：
1. 后一段视频的 prompt 必须描述从前一段最后画面开始的过渡
   - 例如 S2 的 prompt 应包含 "Starting from the expanding shockwave rings of S1..."
2. 或者使用 reference2video 模式，将前一段的最后一帧作为后一段的参考图
3. 在 prompt 中明确指定首帧的色调、构图应与前段尾帧匹配
```

### 方案三：使用单一长视频避免拼接

```markdown
## Single Video Strategy

对于 30 秒以内的成片：
1. 优先生成一个完整的长视频覆盖全片，而非分段生成
2. 如果模型不支持长视频，使用 video extend 功能从第一段视频续接
3. 避免使用多个独立视频的硬拼接
```

### 方案四：分段渲染时在 HTML 中设计过渡帧

```markdown
## Built-in Transition Frames

在 HyperFrames HTML 设计中：
1. seg_A2 的最后 0.5~1s 应设计为 fade-out（渐黑/渐白）
2. seg_B1 的前 0.5~1s 应设计为 fade-in（从黑/白渐入）
3. 这样即使硬拼接，视觉上也有自然过渡
4. 在 GSAP 动画中添加 opacity 从 1→0 和 0→1 的过渡
```

## 其他发现

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 字体不可用 | 已修复 | LLM 使用了 `noto-cjk`（不在字体目录中），后修正为 `noto-sans-cjksc` |
| 非标准分辨率 | Warning | lint 报了 `HF_CONTRACT_NON_STANDARD_RESOLUTION` |
| GSAP 动画重叠 | Warning | 多个元素的 opacity 动画存在微小时间重叠 |

## 总结

项目 56480343747 的拼接断档根因是：**LLM 将成片分为 4 段渲染，其中 A 段和 B 段分别使用了两个独立生成的 AI 视频素材（S1 和 S2），两者的视觉内容完全不同（电光裂痕 vs 字母形态显现），且拼接时未添加任何转场效果，导致 A 段结尾到 B 段开头出现明显的视觉断档。**

核心修复建议：在分段拼接规则中增加约束——当相邻段使用不同视频素材时，必须添加 crossfade 转场效果（0.5~1.0s），或确保后段视频的首帧与前段视频的尾帧在视觉上连贯。
