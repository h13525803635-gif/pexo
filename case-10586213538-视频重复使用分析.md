# 项目 10586213538 — 视频素材重复使用分析

## 问题描述

成片中存在一段视频被重复使用的情况，导致观感上出现明显的画面重复。

## 项目架构

该项目采用了**分段渲染 + 拼接**的架构：

| Segment | 时间范围 | HTML Asset | 渲染 Job |
|---------|---------|------------|----------|
| aurora-seg-00 | 0~10s | asset://a_cauSeFD | aophnmpi |
| aurora-seg-10 | 10~20s | asset://a_uV6wtii | 1taoglko |
| aurora-seg-20 | 20~30s | asset://a_fLTTrWL | c5vas78l |
| aurora-seg-30 | 30~40s | asset://a_jduEnp7 | 6wyjjne1 |
| aurora-seg-40 | 40~50s | asset://a_2U4EWzv | bdehje1g |
| aurora-seg-50 | 50~60s | asset://a_tpBEHNi | 32tey4iu |

6 个 segment 分别渲染后通过 `media_process` 的 `concat` 操作拼接为最终 60 秒成片。

## 根因分析

通过 Langfuse 追踪记录提取各 segment 的 HTML 内容，发现 **同一个视频素材 `asset://a_5Bg5BdC` 在多个 segment 中被重复引用**：

### seg_40_50（aurora-seg-40）中的视频引用

```html
<!-- 主视频：占据 0~9 秒 -->
<video id="vid3" class="screen-vid clip"
  src="asset://a_5Bg5BdC"
  data-start="0" data-duration="9"
  data-media-start="3"
  data-track-index="1"
  muted>

<!-- CTA 背景：占据 9~10 秒，模糊处理 -->
<video id="vid-cta-bg" class="screen-vid clip"
  src="asset://a_5Bg5BdC"
  data-start="9" data-duration="1"
  data-media-start="0"
  data-track-index="5"
  muted
  style="opacity:0.12;filter:blur(24px) brightness(0.4);">
```

### seg_50_60（aurora-seg-50）中的视频引用

```html
<!-- CTA 背景：占据 0~10 秒，模糊处理 -->
<video id="vid-cta-bg" class="screen-vid clip"
  src="asset://a_5Bg5BdC"
  data-start="0" data-duration="10"
  data-media-start="0"
  data-track-index="0"
  muted
  style="opacity:0.12;filter:blur(24px) brightness(0.4);">
```

### 最终成片中的表现

在拼接后的 60 秒成片中，`asset://a_5Bg5BdC` 的画面出现在：

| 成片时间 | 来源 Segment | 用途 | 播放视频的哪段 |
|---------|-------------|------|--------------|
| **40~49s** | seg_40_50 | 主画面（vid3） | 视频第 3~12 秒 |
| **49~50s** | seg_40_50 | CTA 模糊背景 | 视频第 0~1 秒 |
| **50~60s** | seg_50_60 | CTA 模糊背景 | 视频第 0~10 秒 |

**同一段视频素材在成片的 40~60 秒区间内连续出现了 20 秒**，即使 CTA 段做了模糊处理，用户仍然能明显感知到画面重复。

## 根本原因

**LLM 在设计 60 秒分段合成方案时，没有为每个 segment 生成独立的视频素材。** 具体表现为：

1. **视频素材数量不足**：60 秒视频被分为 6 段，但 LLM 生成的视频素材数量少于 6 个，导致后段不得不复用前段的素材
2. **CTA 背景偷懒复用**：LLM 将已有视频素材做模糊处理后作为 CTA 段的背景，而非生成专门的 CTA 背景素材
3. **缺乏素材去重检查**：系统没有在渲染前检测跨 segment 的素材重复使用

## 解决方案

### 短期修复（P0）

在 hyperframes-skill 的分段渲染规则中增加约束：

```
## Multi-Segment Composition Rules:
1. 每个 segment 的主视频（data-track-index 最低层）必须使用独立的视频素材
2. 禁止在相邻 segment 中引用同一个 video asset（即使做了模糊/透明处理）
3. 如果视频素材数量不足以覆盖所有 segment，必须先生成足够的素材再开始渲染
```

### 中期改进（P1）

1. **Lint 规则增强**：在 `lint_composition` 中增加跨 segment 素材重复检测，当同一 asset 出现在多个 segment 的 `<video>` 元素中时，报 warning
2. **素材规划前置**：在分段渲染前，要求 LLM 先输出完整的素材分配计划（哪个 segment 用哪个视频），确保每段有独立素材

### 长期优化（P2）

1. **CTA 段专用素材**：为 CTA/结尾段设计专门的视觉方案（如纯色渐变、品牌动画），而非复用主内容视频
2. **video_generate 批量调用**：支持一次性生成多个风格一致但内容不同的视频片段，确保 60 秒视频的每一段都有独特画面

## 附加发现

- `media_process` 的 concat 操作因路径错误失败（使用了 `file://` 路径而非 `asset://`），说明 LLM 对分段渲染后的文件引用方式理解不正确
- 所有 6 个 segment 的渲染 warnings 中都有 `[gsap_studio_edit_blocked]`，但不影响最终输出

---

**分析时间**：2026-06-23  
**Langfuse Session**：`thread_75127832422_10586213538_01JWJNBDVFVVJQ7KFPQXKW8MBW`  
**关键 Trace IDs**：
- lint (seg_50): `8560546610b5c667ce331ba97fbca7a7`
- lint (seg_40): `dd11f87690dc64cbc45fe23b400f0c5f`
- media_process: `2f62996ccc2a061a2bbed2354d973093`
