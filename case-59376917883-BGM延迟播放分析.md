# 项目 59376917883 — BGM 从第 8 秒才开始播放

## 问题描述

项目 59376917883（Digital Joy - Logo Reveal）是一个 20 秒的黑胶唱片品牌 Logo 揭示动画。成片中 BGM 从第 8 秒左右才开始被听到，前 8 秒几乎无声，导致用户体验不佳。

## Langfuse 追踪时间线

| 时间 (UTC) | 工具 | 操作说明 |
|------------|------|---------|
| 02:17:59 | write_todos | 创建任务计划 |
| 02:18:01~02:18:08 | read_file / list_fonts | 读取项目资源和字体 |
| 02:19:20 | write_file | 写入初始 HTML（无音频元素） |
| 02:19:23 | lint_composition | 失败：路径格式错误（未使用 asset://） |
| 02:20:30 | write_file | 重写 HTML 到正确路径 |
| 02:20:32 | lint_composition | 失败：仍使用本地路径 |
| 02:20:38 | lint_composition | 通过，但报 **[HF_CONTRACT_COMPOSITION_SILENT]** warning |
| 02:20:44 | render_frame | 渲染预览帧 |
| 02:20:52 | analyze_file_content | 分析预览帧效果 |
| **02:21:15** | **music_generate** | **生成 BGM（关键步骤）** |
| 02:21:30 | edit_file | 在 HTML 中添加 `<audio>` 元素（无 id） |
| 02:21:37 | lint_composition | 失败：`[media_missing_id]` 和 `[HF_CONTRACT_AUDIO_ID]` |
| 02:21:44 | edit_file | 修复：添加 `id="bgm"` |
| 02:21:50 | lint_composition | 通过 |
| 02:21:54 | submit_render | 提交渲染 |
| 02:23:07 | show_final_video | 交付最终视频 |

## 根因分析

### 直接原因

最终 HTML 中 BGM 的配置为：

```html
<audio id="bgm" class="clip"
       src="asset://a_Kn78dT7"
       data-start="0" data-duration="20" data-volume="0.85">
</audio>
```

`data-start="0"` 表示 BGM 从第 0 秒开始播放，配置本身没有问题。

### 根本原因：music_generate 的 prompt 导致音频内容前 8 秒近乎静音

LLM 调用 `music_generate` 时使用的 prompt 为：

> "Warm retro ambient music for a vinyl record label logo reveal. Deep analog synthesizer pads, slow evolving textures, subtle vintage tape warmth. Mysterious and soulful, with a gentle golden glow in the sound. **Very slow build from near-silence**, no drums, cinematic and hypnotic. Think late-night FM radio from the 1970s mixed with modern cinematic ambient."

关键短语 **"Very slow build from near-silence"** 指示 ElevenLabs music_v1 模型生成了一段从接近静音开始、极其缓慢渐入的音乐。虽然音频文件从第 0 秒就在技术上"播放"，但前约 8 秒的波形振幅极低，低于人耳可感知阈值，直到第 8 秒左右才逐渐上升到正常音量。

### 问题本质

这不是渲染器或 HTML 配置的 bug，而是 **LLM 在 prompt 设计中过度追求"氛围感"渐入效果**。在一个仅 20 秒的短视频中，前 8 秒无声意味着 40% 的时长被浪费在听不到的音量上，严重影响用户体验。

## 解决方案

### 方案一：在 music_generate prompt 规范中增加约束（推荐）

在系统提示词或 hyperframes-skill 中增加以下规则：

```markdown
## BGM Prompt Guidelines

1. 对于总时长 ≤ 30s 的视频：
   - 禁止使用 "from silence"、"from near-silence"、"slow build" 等描述
   - BGM 必须在前 2 秒内达到目标音量的 50% 以上
   - 可使用 "gentle start" 但不可使用 "from nothing/silence"

2. 对于总时长 > 30s 的视频：
   - 允许最多 3 秒的渐入（fade-in）
   - 渐入时间不得超过总时长的 10%

3. 通用规则：
   - 如果需要渐入效果，使用 "quick fade-in within 1-2 seconds" 而非 "slow build"
   - 生成后应通过 probe_media 验证音频波形，确认前 2 秒内有可听内容
```

### 方案二：渲染后音频验证

在 `submit_render` 成功后、`show_final_video` 交付前，增加音频验证步骤：

```markdown
## Post-Render Audio Validation

1. 调用 probe_media 获取最终视频的音频信息
2. 如果视频有 audio stream，检查：
   - 音频是否在前 3 秒内有有效波形（amplitude > -40dB）
   - 如果前 N 秒静音（N > 总时长的 15%），标记为异常
3. 异常处理：
   - 重新生成 BGM，调整 prompt 移除 "slow build/from silence" 描述
   - 或通过 edit_file 调整 data-start 使音频从有效内容处开始
```

### 方案三：音频后处理（trim silence）

在 music_generate 完成后、嵌入 HTML 前，增加音频预处理步骤：

```markdown
## Audio Pre-processing

1. 对生成的 BGM 调用 probe_media 分析波形
2. 如果检测到前 N 秒为静音/近静音：
   - 设置 data-start 为负值（提前开始）以跳过静音段
   - 或重新生成 BGM 并在 prompt 中明确 "start with audible content immediately"
```

## 影响评估

| 维度 | 评估 |
|------|------|
| 用户感知 | 高 — 前 8 秒无 BGM 让视频显得"空洞" |
| 发生频率 | 中 — 当 LLM 为"氛围类"视频生成 BGM 时容易触发 |
| 修复成本 | 低 — 只需在 prompt 规范中增加约束 |
| 推荐优先级 | **P1** |

## 总结

项目 59376917883 的 BGM 延迟问题根因是 LLM 在 music_generate 的 prompt 中使用了 "Very slow build from near-silence" 描述，导致生成的音频文件前 8 秒振幅极低，人耳无法感知。推荐在系统规范中增加短视频 BGM prompt 约束，确保音乐在前 2 秒内达到可听音量。
